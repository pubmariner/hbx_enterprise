.page-header
  = render 'shared/breadcrumb', crumbs: [ link_to('People', people_path), link_to(@person.name_full, people_path(@person)), 'Compare']
%h4 Person Changes
.row
  .col-md-offset-1.col-md-6
    - unless @diff
      %p No Changes
    - else
      %p
        The table below reflects changes made to
        %strong
          = @person.full_name
          's
        record.
        This update will transmit to the Carrier, so review carefully for: 1) completeness, and 2) accuracy, before clicking Update button.
      %p If you need to make corrections, Click the Back button.
      = render 'change_table', changes: @diff[:addresses_changed]
      %p
        %strong= h('<none given>')
        indicates the attribute value is either
        %strong nil
        or
        %strong blank
= simple_form_for(@person, url: persist_and_transmit_person_path(@person), html: { class: 'form-horizontal' }) do |f|
  = hidden_field_tag(:person, @updates.to_json, id: nil)
  .well.well-sm.well-default
    .form-actions
      = f.submit "Confirm Changes", :class => 'btn btn-primary'
      = link_to "Back", 'javascript:history.back()', :class => 'btn btn-default'
